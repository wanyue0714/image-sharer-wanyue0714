<h1>Log In</h1>

<%= simple_form_for @session_form, url: sessions_path do |f| %>
  <%= f.input :email, label: 'Email' %>
  <%= f.input :password, label: 'Password' %>
  <%= f.button :submit, 'Log In' %>
<% end %>

<p><%= link_to('Sign Up', new_user_path) %></p>
